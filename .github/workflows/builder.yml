name: Clean WebView APK Builder

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          build-tools-version: '33.0.2'
          license-accepted: true

      - name: Create Minimal Android App Structure
        run: |
          # تجهيز مجلدات التطبيق الأساسية
          mkdir -p app/src/main/assets
          mkdir -p app/src/main/java/com/mrxboss/app
          mkdir -p app/src/main/res/values

          # التحقق من وجود ملف الـ HTML ووضعه في الأصول
          if [ ! -f index.html ]; then
            echo "<h1>MrXboss App</h1>" > index.html
          fi
          cp index.html app/src/main/assets/index.html

          # إنشاء ملف إعدادات المشروع الرئيسي
          cat << 'EOF' > build.gradle
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath 'com.android.tools.build:gradle:7.4.2'
              }
          }
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          EOF

          # إنشاء ملف إعدادات الحزم
          cat << 'EOF' > settings.gradle
          rootProject.name = "MrXbossApp"
          include ':app'
          EOF

          # إنشاء ملف إعدادات تطبيق الأندرويد
          cat << 'EOF' > app/build.gradle
          apply plugin: 'com.android.application'
          android {
              compileSdkVersion 33
              defaultConfig {
                  applicationId "com.mrxboss.app"
                  minSdkVersion 21
                  targetSdkVersion 33
                  versionCode 1
                  versionName "1.0"
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
          }
          EOF

          # إنشاء ملف الواجهة الأساسي AndroidManifest
          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              package="com.mrxboss.app">
              <uses-permission android:name="android.permission.INTERNET" />
              <application android:label="Mr Xboss" android:usesCleartextTraffic="true">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # إنشاء كود تشغيل المتصفح الداخلي MainActivity
          cat << 'EOF' > app/src/main/java/com/mrxboss/app/MainActivity.java
          package com.mrxboss.app;
          import android.os.Bundle;
          import android.webkit.WebView;
          import android.webkit.WebViewClient;
          import androidx.appcompat.app.AppCompatActivity;
          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  WebView webView = new WebView(this);
                  webView.getSettings().setJavaScriptEnabled(true);
                  webView.getSettings().setDomStorageEnabled(true);
                  webView.setWebViewClient(new WebViewClient());
                  webView.loadUrl("file:///android_asset/index.html");
                  setContentView(webView);
              }
          }
          EOF

      - name: Build with Gradle
        run: |
          gradle wrapper --gradle-version 7.5
          ./gradlew assembleDebug

      - name: Extract Final APK
        run: |
          mkdir -p output
          cp app/build/outputs/apk/debug/app-debug.apk output/MrXbossApp.apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: compiled-apk
          path: output/MrXbossApp.apk
